<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site_name }}</title>
    <link href="/" rel="alternate"/>
    <link href="/feed.xml" rel="self"/>
    <id>/</id>
    <updated>{{ posts[0].published_at | default(value='') }}T00:00:00Z</updated>
    {% for post in posts %}
    <entry>
        <title>{{ post.title }}</title>
        <link href="/{{ post.slug }}.html"/>
        <id>/{{ post.slug }}.html</id>
        <updated>{{ post.published_at }}T00:00:00Z</updated>
        <summary>{{ post.excerpt | default(value='') }}</summary>
    </entry>
    {% endfor %}
</feed>
